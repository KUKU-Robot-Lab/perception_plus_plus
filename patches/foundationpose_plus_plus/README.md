# Upstream patches

Store ordered `git format-patch` files here. Apply them after submodule checkout
and document each patch's upstream issue and supported revision. The initial
framework does not modify the pinned upstream checkout.

